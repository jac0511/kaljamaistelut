<p id="notice"><%= notice %></p>

<h2><%= @brewery.name %></h2>

<p><i>Established in <%= @brewery.year %></i></p>
<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>
<p>
  <% if @brewery.ratings.empty? %>
    Any beers from the brewery has not yet been rated!
  <% else %>
    The beers of the brewery have <%= pluralize(@brewery.ratings.count, 'rating') %>, on average <%= @brewery.average_rating %>.
  <% end %>
</p>
<p>
  <%= link_to 'list of beers', beers_path %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
